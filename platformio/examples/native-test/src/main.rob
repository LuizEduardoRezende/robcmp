int16 main(){ 
	a = 1;

	b = (-1); // -1
	c = (-a); // -1
	d = (a-a); // 0
	e = (a-1); // 0
	f = (1 - -a); // 2


	if a == 1 { g = 199 ;}

	if (a != 1)  { return 1; }
	if (b != -1) { return 1; }
	if (c != -1) { return 1; }
	if (d != 0)  { return 1; }
	if (e != 0)  { return 1; }
	if (f != 2)  { return 1; }

	return 0;
}
